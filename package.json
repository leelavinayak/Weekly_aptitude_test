{
  "name": "vemu-quiz-craft",
  "version": "1.0.0",
  "description": "Root configuration for Vemu Quiz Craft deployment",
  "main": "server/server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install-server": "cd server && npm install",
    "postinstall": "npm run install-server",
    "install-client": "cd client && npm install",
    "build": "npm run install-server && npm run build:client",
    "build:client": "cd client && npm install && npm run build",
    "start": "node server/server.js",
    "start:prod": "NODE_ENV=production node server/server.js",
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev"
  },
  "dependencies": {},
  "devDependencies": {}
}
